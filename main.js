const a = 'hola';
const num1 = 14;
const rama = 'estamos en la rama login';

const btn = document.getElementById("btn");

btn.addEventListener('click', () =>{
    const name = document.getElementById("name");
    name.textContent = "Henry";
})